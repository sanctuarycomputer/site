import Ember from 'ember';
import c, { vars } from 'site/lib/vudu';
import {
  isAnimating,
  finish,
  timeSpent,
  animate,
  stop,
  Promise,
} from "liquid-fire";

export default function fadeWhenReady(opts={}) {
  debugger;


};
